#pragma once

#include <map>
#include "Singleton.h"
#include "Types.h"

class EquipManager : public Singleton<EquipManager>
{
private:
    // TODO: 根据ai的代码补全
private:
    friend class Singleton<EquipManager>;
    EquipManager();
};

